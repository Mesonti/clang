#include <stdio.h>
#include <stdlib.h>

#define N 100

int main()
{
    int a, b, c, d, e, total;
    a = 2;
    b = 13;
    c = 7;
    d = 19;
    e = -4;
    total = c % d - e;
    // printf("%d", total);

    int array[] = {1, 2, 3, 4, 5, 6, 7};
    char arrayS[] = {0};
    char symbol = '*';

    // заполнение массива
    // for (int j = 0; j < N; ++j)
    // {
    //     arrayS[j] += symbol;
    // }

    // печать массива
    // for (int i = 0; i < arrayS[i]; ++i)
    // {
    //     printf("%c", arrayS[i]);
    // }

    for (int i = 0; i < array[i]; ++i)
    {
        arrayS[i] = +symbol;
    }

    
    for (int r = 1; r < array[r]; r++)
    {
        if (array[r] > 0)
        {
            printf("%d: %c\n", r, arrayS[r]);
        }
        
    }
}